
<h1 style="color:#003333"> Hi James,Welcome to CloudTags</h1>
%h2
  - if @profiles
    
    <h2 style="color:#FF0000">Nice to see you again!</h2>
  - else
   
- if session[:access_token]
  %p
    Your access token is
    %input{:type => :text, :value => session[:access_token], :readonly => :readonly}
    %a(href="/logout") Log out
%ul
  
  - %w[facebook twitter linkedin google github youtube wordpress gmail instagram flicker dropbox foursquare meetup dwolla klout 37signals].each do |service|
    %li
      = service.capitalize
      - if @profiles && @profiles[service]
        is connected as
        = @profiles[service]
      - else
        is not connected please click here.
        %a(href="#{auth_url(service)}") Connect
