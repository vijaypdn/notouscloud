%h1 Different Services connected using Singly Service
%h2
  - if @profiles
    Nice to see you again!
  - else
   
- if session[:access_token]
  %p
    Your access token is
    %input{:type => :text, :value => session[:access_token], :readonly => :readonly}
    %a(href="/logout") Log out
%ul
  
  - %w[facebook twitter linkedin].each do |service|
    %li
      = service.capitalize
      - if @profiles && @profiles[service]
        is connected as
        = @profiles[service]
      - else
        is not connected please click here.
        %a(href="#{auth_url(service)}") Connect
